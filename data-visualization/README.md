# Algorithm implementation based on the OceanProtocol ecosystem

Build and push

```bash
$ docker buildx build --platform linux/amd64,linux/arm64 -t registry.agrospai.udl.cat/library/tareando-aggregate-benchmarking:{version} . --push
```

_Algorithm details_
