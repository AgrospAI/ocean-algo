# Algorithm "monolith" implementation based on the OceanProtocol ecosystem

Copy the full `python` directory and implement the algorithm in the `src/implementation/` subdirectory, if needed import other files using relative routes.

This template is using `uv` as python's package manager, letting the Dockerfile make the building and running steps from the `uv.lock` and `pyproject.toml`.
