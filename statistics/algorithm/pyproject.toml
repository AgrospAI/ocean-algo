[project]
name = "algorithm"
version = "0.1.0"
description = ""
authors = [{ name = "AgrospAI", email = "<your-name>@udl.cat" }]
requires-python = ">=3.9"
readme = "README.md"
license = { file = "LICENSE" }
dependencies = [
    "pytest>=8.3.4,<9",
    "pandas>=2.2.3,<3",
    "oceanprotocol-job-details>=0.0.9",
]

[tool.uv]
package = false
